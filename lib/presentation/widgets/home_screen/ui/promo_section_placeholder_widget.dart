import 'package:flutter/material.dart';

/// Виджет состояния загрузки секции на главном экране
class PromoSectionPlaceholderWidget extends StatelessWidget {
  const PromoSectionPlaceholderWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
