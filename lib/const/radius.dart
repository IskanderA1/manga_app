part of 'theme.dart';

///Радиусы

final BorderRadius kDefRadius = BorderRadius.circular(8);

final BorderRadius kRadius1 = BorderRadius.circular(24);

const BorderRadius kRadius1Bottom = BorderRadius.vertical(
  bottom: Radius.circular(24),
);

const BorderRadius kRadius1Top = BorderRadius.vertical(
  top: Radius.circular(24),
);
